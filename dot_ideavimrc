set number relativenumber
set surround
set commentary
set ideajoin
set ideamarks
set wrap
set linebreak

set clipboard^=unnamedplus

source ~/.lazy-idea/lazy-idea.vim

" Custom overrides (after lazy-idea so they take precedence)
nnoremap Y y$
