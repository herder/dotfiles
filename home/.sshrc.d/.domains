DOMAIN=`grep domain /etc/resolv.conf | cut -d ' ' -f2`
for i in `ls $SSHHOME/.sshrc.d/domains/*` ; do
  if [[ $DOMAIN == *"`basename $i`" ]]; then
    echo "Applying config for $DOMAIN from file $i"
    source $i
  fi
done
